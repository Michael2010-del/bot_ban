# 🤖 Security Chat Bot - Telegram Модератор
**Python | Telegram Bot API | Модерация чата | Автоматический бан**

## 📖 О проекте
Security Chat Bot — это автоматизированный бот-модератор для Telegram чатов, предназначенный для поддержания порядка и безопасности. Бот автоматически банит пользователей за нарушение правил, проверяет новые участников и защищает чат от спама.

## ✨ Основные возможности
- 🔒 **Автоматическая модерация** - мгновенная реакция на нарушения
- 🚫 **Бан за ссылки** - защита от спама и фишинга
- 🎤 **Блокировка голосовых** - поддержание текстового формата общения
- 👋 **Автоприветствие** - автоматическое одобрение новых участников
- ⚖️ **Умная проверка** - администраторы защищены от случайных банов
- 🔧 **Ручное управление** - команда `/ban` для админов
- 🛡️ **Многоуровневая защита** - разные правила для админов и пользователей

## 🏗️ Архитектура проекта

### Структура файлов:
```
├── bot.py              # Основная логика бота
├── config.py           # Конфигурация (токен)
└── requirements.txt    # Зависимости (опционально)
```

### Команды бота:
| Команда | Описание | Кто может использовать |
|---------|----------|------------------------|
| `/start` | Начало работы с ботом | Все пользователи |
| `/ban` | Забанить пользователя | Только администраторы |

### Правила модерации:
1. **Запрещены ссылки** - мгновенный бан для обычных пользователей
2. **Запрещены голосовые** - автоматический бан за голосовые сообщения
3. **Защита админов** - администраторов нельзя забанить
4. **Приветствие** - автоматическое одобрение новых участников

## 🚀 Установка и запуск

### Требования:
- Python 3.7+
- Аккаунт Telegram
- Права администратора в целевом чате
- Токен бота от @BotFather

### Установка зависимостей:
```bash
pip install pyTelegramBotAPI
```

### Настройка конфигурации:
1. Получите токен у [@BotFather](https://t.me/BotFather)
2. Вставьте токен в `config.py`:
```python
token = "ВАШ_ТОКЕН_ЗДЕСЬ"
```

### Добавление бота в чат:
1. Создайте группу или супергруппу
2. Добавьте бота как участника
3. Назначьте боту права администратора:
   - ✅ **Удаление сообщений**
   - ✅ **Блокировка пользователей**
   - ✅ **Приглашение пользователей через ссылку**

### Запуск бота:
```bash
python bot.py
```

## 🎮 Использование бота

### Примеры работы:

#### 1. Приветствие нового участника:
```
👤 [Новый пользователь присоединяется к чату]

🤖 Бот:
Я приветствую тебя новый пользователь!
[Пользователь автоматически одобрен]
```

#### 2. Бан за ссылку:
```
👤 Обычный пользователь:
Привет! Посмотрите этот сайт: https://example.com

🤖 Бот:
Пользователь @username был забанен за отправку ссылки.
[Пользователь мгновенно забанен]
```

#### 3. Бан за голосовое сообщение:
```
👤 Обычный пользователь: [Отправляет голосовое сообщение]

🤖 Бот:
Пользователь @username был забанен за отправку голосового сообщения.
[Пользователь мгновенно забанен]
```

#### 4. Ручной бан администратором:
```
👤 Нарушитель: [Нарушает правила]
👤 Администратор: [Отвечает на сообщение] /ban

🤖 Бот:
Пользователь @нарушитель был забанен.
[Только если пользователь не администратор]
```

#### 5. Защита администраторов:
```
👤 Пользователь: [Пытается забанить админа через /ban]
🤖 Бот: Невозможно забанить администратора.
```

## 🛠️ Технические детали

### Используемые технологии:
| Библиотека | Назначение | Версия |
|------------|------------|---------|
| **pyTelegramBotAPI** | Основной фреймворк для Telegram ботов | 4.14+ |
| **Python 3.7+** | Язык программирования | - |

### Ключевые функции:

#### 1. **Проверка новых участников**:
```python
@bot.message_handler(content_types=['new_chat_members'])
def make_some(message):
    bot.send_message(message.chat.id, 'Я приветствую тебя новый пользователь!')
    bot.approve_chat_join_request(message.chat.id, message.from_user.id)
```

#### 2. **Ручной бан**:
```python
@bot.message_handler(commands=['ban'])
def ban_user(message):
    if message.reply_to_message:  # Только в ответ на сообщение
        # Проверка прав пользователя
        user_status = bot.get_chat_member(chat_id, user_id).status
        if user_status == 'administrator' or user_status == 'creator':
            bot.reply_to(message, "Невозможно забанить администратора.")
        else:
            bot.ban_chat_member(chat_id, user_id)
```

#### 3. **Автобан за ссылки**:
```python
def check_messages(message):
    # Пропускаем администраторов
    if user_status in ['administrator', 'creator']:
        return
    
    # Проверяем ссылки
    if message.text and ("https://" in message.text or "http://" in message.text):
        bot.ban_chat_member(chat_id, user_id)
```

#### 4. **Автобан за голосовые**:
```python
@bot.message_handler(content_types=['voice'])
def ban_voice_messages(message):
    if user_status not in ['administrator', 'creator']:
        bot.ban_chat_member(chat_id, user_id)
```

### Статусы пользователей в Telegram:
- `creator` - Создатель чата
- `administrator` - Администратор
- `member` - Обычный участник
- `restricted` - Ограниченный участник
- `left` - Покинул чат
- `kicked` - Забанен

## 🔧 Настройка и кастомизация

### Изменение правил модерации:

#### 1. Добавить исключения для ссылок:
```python
# Разрешить определенные домены
allowed_domains = ['t.me', 'telegram.org', 'youtube.com']
if any(domain in message.text for domain in allowed_domains):
    return  # Пропустить разрешенные ссылки
```

#### 2. Добавить предупреждения перед баном:
```python
# Счетчик предупреждений
warnings[user_id] = warnings.get(user_id, 0) + 1
if warnings[user_id] >= 3:
    bot.ban_chat_member(chat_id, user_id)
    bot.send_message(chat_id, f"Пользователь @{username} забанен после 3 предупреждений.")
```

#### 3. Временный бан вместо перманентного:
```python
# Бан на 1 час (в секундах)
from datetime import datetime, timedelta
until_date = datetime.now() + timedelta(hours=1)
bot.ban_chat_member(chat_id, user_id, until_date=int(until_date.timestamp()))
```

#### 4. Логирование действий:
```python
import logging
logging.basicConfig(filename='moderation.log', level=logging.INFO)

def log_action(action, user_id, username, reason):
    logging.info(f"{datetime.now()} - {action}: @{username} ({user_id}) - {reason}")
```

### Добавление новых правил:

#### 1. Бан за плохие слова:
```python
bad_words = ['плохое_слово1', 'плохое_слово2']
if any(word in message.text.lower() for word in bad_words):
    bot.ban_chat_member(chat_id, user_id)
```

#### 2. Ограничение на длину сообщений:
```python
if len(message.text) > 1000:  # Слишком длинное сообщение
    bot.delete_message(chat_id, message.message_id)
    bot.send_message(chat_id, f"@{username}, пожалуйста, пишите короче.")
```

#### 3. Защита от флуда:
```python
from collections import defaultdict
import time

user_messages = defaultdict(list)
current_time = time.time()

# Удаляем старые сообщения
user_messages[user_id] = [t for t in user_messages[user_id] if current_time - t < 60]

# Проверяем лимит (например, 10 сообщений в минуту)
if len(user_messages[user_id]) > 10:
    bot.ban_chat_member(chat_id, user_id)
```

## ⚙️ Права доступа бота

### Необходимые права:
- ✅ **Удаление сообщений** - для удаления нарушений
- ✅ **Блокировка пользователей** - для бана нарушителей
- ✅ **Приглашение пользователей** - для одобрения новых участников
- ✅ **Закрепление сообщений** - для важных объявлений
- ❌ **Анонимность** - не требуется
- ❌ **Изменение информации чата** - не требуется

### Настройка прав через @BotFather:
```
/setprivacy - Отключить приватность
/setcommands - Настроить команды
/setuserpic - Установить аватар
/setdescription - Добавить описание
```

## 🚨 Возможные проблемы и решения

### Проблема 1: Бот не банит пользователей
**Решение:** Убедитесь, что:
- Бот имеет права администратора
- Права включают "Блокировка пользователей"
- Пользователь не является администратором

### Проблема 2: Бот банит администраторов
**Решение:** Проверьте код проверки статуса:
```python
if user_status in ['administrator', 'creator']:
    return  # Пропустить администраторов
```

### Проблема 3: Ложные срабатывания на ссылки
**Решение:** Добавьте исключения:
```python
# Пропускать ссылки на Telegram
if "t.me" in message.text:
    return
```

### Проблема 4: Бот не работает в больших чатах
**Решение:** Используйте вебхуки вместо polling:
```python
bot.remove_webhook()
bot.set_webhook(url="https://your-domain.com/webhook")
```

## 📊 Статистика и мониторинг

### Добавление статистики:
```python
stats = {
    'total_bans': 0,
    'link_bans': 0,
    'voice_bans': 0,
    'manual_bans': 0
}

# При каждом бане
stats['total_bans'] += 1
stats['link_bans'] += 1  # Или другой тип
```

### Команда для статистики:
```python
@bot.message_handler(commands=['stats'])
def show_stats(message):
    stats_text = f"""
📊 Статистика модерации:
├ Всего банов: {stats['total_bans']}
├ За ссылки: {stats['link_bans']}
├ За голосовые: {stats['voice_bans']}
└ Ручные: {stats['manual_bans']}
"""
    bot.reply_to(message, stats_text)
```

## 🔐 Безопасность

### Рекомендации по безопасности:
1. **Храните токен в секрете** - используйте переменные окружения
2. **Ограничьте доступ к боту** - только доверенные администраторы
3. **Регулярно обновляйте** - следите за обновлениями библиотек
4. **Резервное копирование** - сохраняйте важные данные
5. **Мониторинг логов** - отслеживайте подозрительную активность

### Переменные окружения для безопасности:
```python
import os
token = os.getenv('TELEGRAM_BOT_TOKEN', 'ваш_токен_по_умолчанию')
```

---

**🛡️ Security Chat Bot — надёжный страж вашего Telegram чата!**


